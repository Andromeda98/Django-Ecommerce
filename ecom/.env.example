# Example .env for local development
# Copy this file to .env (or set variables in your shell) and NEVER commit the real .env file

# Django secret key (production must use a secure, random, private key)
DJANGO_SECRET_KEY=your-very-secret-key-here

# Django debug mode
DJANGO_DEBUG=True
